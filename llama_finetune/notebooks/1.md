[grpo_with_trl_ifstruct](https://colab.research.google.com/github/Liquid4All/cookbook/blob/main/finetuning/notebooks/grpo_with_trl_ifstruct.ipynb#scrollTo=cf52eec9)

In this notebook, we will walk through how to fine-tune an LFM2.5-350M model with GRPO using the TRL library.

The goal of this notebook is to train schema compliance only, as described in the IFStruct blog post. The rewards do not judge whether extracted values are semantically correct. Instead, they only judge whether the generated response follows the requested JSON format and JSON Schema.

This notebook covers the training pipeline discussed in the accompanying blog post "accompanying blog post".
